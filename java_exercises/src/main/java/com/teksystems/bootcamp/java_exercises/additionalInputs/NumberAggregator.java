package com.teksystems.bootcamp.java_exercises.additionalInputs;

public class NumberAggregator {
    public int total;

    public int addNumber(int userInput) {
        return total = (total + userInput);
    }
}